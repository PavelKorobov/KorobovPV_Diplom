
#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ДатаПриИзменении(Элемент)
	Объект.Дата = КонецМесяца(Объект.Дата); 
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Заполнить(Команда)
	ЗаполнитьНаСервере();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ЗаполнитьНаСервере()
	
	Остатки = РегистрыНакопления.ВКМ_ВзаиморасчетыССотрудниками.Остатки(Новый Граница(Объект.Дата, ВидГраницы.Включая));
	Если Остатки.Количество() = 0 Тогда
		Объект.Выплаты.Очистить();
		Возврат;
	КонецЕсли;
	
	Объект.Выплаты.Очистить();
	Для Каждого Строка Из Остатки Цикл
		НоваяСтрокаТЧ = Объект.Выплаты.Добавить();
		НоваяСтрокаТЧ.Сотрудник = Строка.Сотрудник;
		НоваяСтрокаТЧ.Сумма = Строка.Сумма;
	КонецЦикла;

КонецПроцедуры

#КонецОбласти